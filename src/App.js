import React from "react";
import "./App.css";

function App() {
  return (
    <h1
      className="App"
      style={{
        backgroundColor: "var(--subtle-background)",
        color: "var(--text-color)",
      }}
    >
      App
    </h1>
  );
}

export default App;
